# DsAlgorithms

## Build

First, make sure you've installed CMake with version >= 3.12

    mkdir build
    cd build
    cmake ..
    cmake --build .

## Run tests

Make sure your working directory is `build/`

    ./suffix_array_test